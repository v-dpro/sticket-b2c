// Route stub for the center LOG tab slot. The tab bar renders a custom
// button (see _layout.tsx) that opens the log flow; this screen is never
// shown.

export default function LogFabStub() {
  return null;
}
